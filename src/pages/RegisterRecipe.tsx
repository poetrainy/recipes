import { FC } from "react";

// eslint-disable-next-line react-refresh/only-export-components
export const action = async () => {};

const RegisterRecipe: FC = () => <></>;

export default RegisterRecipe;
